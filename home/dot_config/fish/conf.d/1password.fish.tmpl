# 1Password service account token — used by the op CLI to skip the desktop app
# (avoids biometric prompts in non-interactive shells, e.g. Claude Code Bash tool).
# Token sourced from the "Service Account Auth Token: claude-code" item in the
# Automation vault. Never edit the rendered value directly — re-run chezmoi apply.
set -gx OP_SERVICE_ACCOUNT_TOKEN "{{ onepasswordRead "op://rzkj2sqywuyot4rmrls4cfbqpi/ha4mk3prjlrdovkoxy5fepooey/credential" "7PZJXY3IIFEJDHSPIUCD526DC4" }}"
